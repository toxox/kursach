<%= simple_form_for @genre, :html => { :class => 'form-horizontal' } do |f| %>

  <%= f.input :name %>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                genres_path, :class => 'btn' %>
  </div>
<% end %>
