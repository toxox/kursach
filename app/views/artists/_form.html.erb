<%= simple_form_for @artist, :html => { :class => 'form-horizontal' } do |f| %>

  <%= f.input :name %>
  <%= f.input :date_started, as: :date, start_year: 1900, end_year: Date.today.year %>
  <%= f.input :n_of_participants %>
  <!-- <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :date_started, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :date_started, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :n_of_participants, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :n_of_participants, :class => 'text_field' %>
    </div>
  </div> -->

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                artists_path, :class => 'btn' %>
  </div>
<% end %>
